# Additional Pixi platforms are valid, but v0 supports only Linux with optional Apple Silicon.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "win-64"]

[dependencies]
zlib = "1.3.*"
