# Solve groups couple environment composition; profile v0 intentionally excludes that machinery.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[environments]
default = { features = [], solve-group = "shared" }
