# This object selects the same default feature in Pixi, but v0 accepts only the simpler [] form.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[environments]
default = { features = [] }
