# The empty table is valid Pixi but is neither omission nor the supported explicit default form.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[environments]
