# The feature is unused, deliberately demonstrating that profile shape—not selection—rejects it.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[feature.analysis.dependencies]
samtools = "1.21.*"
