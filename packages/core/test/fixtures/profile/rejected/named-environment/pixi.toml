# Named environments imply another distributable target, even when they reuse the default feature.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[environments]
analysis = []
