# Valid Pixi composition, but profile v0 never permits removing the default feature.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[environments]
default = { features = [], no-default-feature = false }
