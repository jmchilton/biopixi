# Pixi's explicit spelling for the otherwise implicit default environment remains in profile.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[environments]
default = []
