# Only effective dependency tables participate: this macOS target is unused by the Linux project.
[workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
zlib = "1.3.*"

[target.osx-arm64.pypi-dependencies]
cowsay = "==6.1"
