# @biopixi/cli

The `biopixi` command-line interface.

## Installation

```bash
npm install --global @biopixi/cli
```

## Usage

```bash
biopixi grade .
biopixi grade project-a project-b --min-level 3
```

Without an explicit directory, `biopixi grade` grades the current directory.
