{
  "name": "biopixi",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test && pnpm test:profile-fixtures",
    "test:profile-fixtures": "node scripts/validate-profile-fixtures.mjs",
    "lint": "pnpm -r lint",
    "lint:unused": "knip",
    "lint:unused:ci": "knip --include files,dependencies,unlisted,binaries,unresolved",
    "check:packages": "pnpm -r lint:pkg && pnpm -r check:types",
    "format": "prettier --check --ignore-unknown README.md PROFILE.md package.json pnpm-workspace.yaml tsconfig.json eslint.config.js vitest.config.ts typedoc.json knip.json scripts docs .github .changeset && pnpm -r format",
    "format-fix": "prettier --write --ignore-unknown README.md PROFILE.md package.json pnpm-workspace.yaml tsconfig.json eslint.config.js vitest.config.ts typedoc.json knip.json scripts docs .github .changeset && pnpm -r format-fix",
    "check": "pnpm build && pnpm lint && pnpm format && pnpm typecheck && pnpm test && pnpm schema:check && pnpm lint:unused:ci && pnpm check:packages",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "schema": "node scripts/generate-schema.mjs",
    "schema:check": "node scripts/generate-schema.mjs --check",
    "docs:api": "typedoc",
    "docs:profile": "node scripts/sync-profile-doc.mjs",
    "docs:dev": "pnpm docs:profile && docsify serve docs",
    "docs:build": "pnpm build && pnpm docs:profile && pnpm docs:api"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "@eslint/js": "^10.0.1",
    "docsify-cli": "^4.4.4",
    "eslint": "^10.1.0",
    "knip": "^6.4.1",
    "prettier": "^3.8.1",
    "publint": "^0.3.18",
    "ts-json-schema-generator": "^2.9.0",
    "typedoc": "^0.28.18",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.57.2",
    "vitest": "^4.1.4"
  }
}
