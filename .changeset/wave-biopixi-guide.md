---
---

Documentation only, so nothing additional to release. `packages/wave-cli/README.md` no longer says
biopixi wraps no local builder, now that `mulled-biopixi` ships; that corrected text reaches npm
with the `@biopixi/wave-cli` minor already queued in `wave-biopixi.md`.
