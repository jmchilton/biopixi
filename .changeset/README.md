# Changesets

This directory contains release notes consumed by
[Changesets](https://github.com/changesets/changesets).

For a user-visible package change, run `pnpm changeset`, select the affected packages, and commit
the generated Markdown file with the implementation.
